<Window x:Class="DownloadYou.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloadYou.Presentation"
        mc:Ignorable="d"
        Title="DownloadYou — Prototipo técnico" Height="480" Width="760"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,4">
            <TextBlock Text="Fase 1 — Prototipo técnico" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="Comprueba que la app puede invocar yt-dlp y ffmpeg como procesos externos y leer su salida en tiempo real."
                       Foreground="#666" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,8">
            <Button Content="Probar motores (yt-dlp / ffmpeg)"
                    Command="{Binding RunDiagnosticsCommand}"
                    Padding="14,6"/>
            <TextBlock Text="{Binding Summary}" VerticalAlignment="Center" Margin="16,0,0,0" FontWeight="SemiBold"/>
        </StackPanel>

        <Separator Grid.Row="2" Margin="0,4,0,8"/>

        <ListBox Grid.Row="3" ItemsSource="{Binding LogLines}"
                  FontFamily="Consolas" FontSize="12" Background="#111" Foreground="#ddd"
                  BorderThickness="0"/>
    </Grid>
</Window>
